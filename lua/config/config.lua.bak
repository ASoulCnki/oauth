local redisConfig = {
    host = "127.0.0.1",
    port = 6379,
    db_index = 0,
    password = nil,
    timeout = 1000,
    pool_size = 100,
    keepalive = 30000
}

local Auth = {
    headersName = "authorization",
    enableBlackList = false, -- enable blackList
    expireTokenTime = 120, -- seconds
    expireTempCodeTime = 1200,
    expireSessionTime = 86400 * 7, -- session expire time
    blackUidList = {} -- unused, uid tuple
}

-- add you cookie here
local cookie = ""

local _M = {
    redisConfig = redisConfig,
    Auth = Auth,
    cookie = cookie
}

return _M
